# คู่มือติดตั้ง

ระบบแบ่งเป็นสองส่วนที่ทำงานแยกกันได้ ถ้าไม่ตั้งค่าส่วนที่ 2 โปรแกรมยังใช้งานได้ครบเหมือนเดิม เพียงแต่ปุ่มที่เกี่ยวกับทะเบียนกลางจะถูกซ่อนไว้

| ส่วน | ไฟล์ | ทำอะไร |
|---|---|---|
| หน้าเว็บ | `index.html` `app.js` `style.css` | เปิด PDF ประทับตรา ดาวน์โหลด/พิมพ์ลงเครื่อง |
| ทะเบียนกลาง | `Code.gs` | บันทึกลงชีต เก็บไฟล์ใน Drive แจกเลขทะเบียน เก็บค่าตั้งค่าหน่วยงาน |
| เข้าระบบและแชร์ผ่าน LINE | `liff-share.js` | ล็อกอิน LINE ก่อนเข้าหน้าเว็บ และแชร์เอกสารเป็น Flex Message |
| การ์ดเอกสาร | `flex-doc.js` | หน้าตาการ์ดและการเข้ารหัสข้อมูลใส่ลิงก์ ใช้ร่วมกันทั้งสองหน้า |
| หน้าส่งต่อ | `share.html` `share.js` | ผู้รับกดปุ่มบนการ์ดแล้วส่งต่อให้คนอื่นได้อีก |

---

## ส่วนที่ 1 — วางหน้าเว็บบน GitHub Pages

1. อัปโหลดทุกไฟล์ขึ้น repository — `index.html` `app.js` `style.css` `flex-doc.js` `liff-share.js` `share.html` `share.js`
2. ไปที่ **Settings → Pages** เลือก Branch: `main` โฟลเดอร์ `/ (root)` แล้วกด Save
3. รอราวหนึ่งนาที จะได้ลิงก์รูปแบบ `https://ชื่อบัญชี.github.io/ชื่อ-repo/`

ลิงก์นี้คือ **Endpoint URL** ที่ต้องนำไปใส่ใน LIFF ในส่วนที่ 3 ด้วย

> หน้าเว็บถูกกั้นด้วยการล็อกอิน LINE ตั้งแต่เปิด ถ้ายังไม่ได้ทำส่วนที่ 3 จะเข้าไม่ได้

---

## ส่วนที่ 2 — ทะเบียนกลางบน Google Sheet และ Drive

### 2.1 เตรียมที่เก็บ

1. สร้างสเปรดชีตใหม่ ตั้งชื่ออะไรก็ได้
2. สร้างโฟลเดอร์ใน Drive สำหรับเก็บไฟล์ PDF แล้วคัดลอกรหัสจาก URL

   ```
   https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrSt
                                          └─────── FOLDER_ID ───────┘
   ```

### 2.2 ใส่สคริปต์

1. ในสเปรดชีต เลือก **ส่วนขยาย → Apps Script**
2. ลบโค้ดเดิมทั้งหมด วางเนื้อหาจาก `Code.gs` ลงไป
3. แก้ `CONFIG` ด้านบนไฟล์

   ```js
   FOLDER_ID: '1AbCdEfGhIjKlMnOpQrSt',   // โฟลเดอร์จากขั้นตอน 2.1
   API_KEY: 'thaigham-2569-x8k2m9',      // ตั้งข้อความสุ่มของตัวเอง
   ```

4. กด **บันทึก** แล้วเลือกฟังก์ชัน `setupSheet` กด **เรียกใช้** ครั้งแรกจะขออนุญาตเข้าถึง Drive และชีต ให้กดอนุญาต
5. เลือกฟังก์ชัน `testConfig` กดเรียกใช้อีกครั้ง ถ้าตั้งค่าครบจะเห็นชื่อชีต ชื่อโฟลเดอร์ และเลขทะเบียนถัดไปใน Log

### 2.3 เผยแพร่เป็น Web App

1. กด **ทำให้ใช้งานได้ → การทำให้ใช้งานได้ใหม่ → ประเภท: เว็บแอป**
2. ตั้งค่าให้ตรงตามนี้ ทั้งสองบรรทัดสำคัญมาก

   | ช่อง | ค่า |
   |---|---|
   | ดำเนินการในชื่อ | ฉัน (บัญชีของคุณ) |
   | ผู้ที่มีสิทธิ์เข้าถึง | **ทุกคน** |

3. คัดลอก URL ที่ได้ ต้องลงท้ายด้วย `/exec`

### 2.4 เชื่อมหน้าเว็บกับสคริปต์

เปิด `app.js` หาบล็อก `const CLOUD` (อยู่ใกล้ท้ายไฟล์) แล้วใส่ค่าสองบรรทัด

```js
const CLOUD = {
  webAppUrl: 'https://script.google.com/macros/s/AKfy...ยาว.../exec',
  apiKey: 'thaigham-2569-x8k2m9'      // ต้องตรงกับใน Code.gs
};
```

อัปโหลด `app.js` ขึ้น GitHub อีกครั้ง รีเฟรชหน้าเว็บ ปุ่มเหล่านี้จะปรากฏขึ้นเอง

- ไอคอน Drive บนแถบบน — อัปโหลด PDF ขึ้นโฟลเดอร์ (ไม่แตะทะเบียน)
- ปุ่ม **บันทึกลงทะเบียน** ในแผงข้อมูลลงรับ (บันทึกลงชีตพร้อมอัปไฟล์ขึ้น Drive)
- ปุ่ม **#** ข้างช่องเลขทะเบียนรับ — ขอเลขถัดไปจากชีต
- ลิงก์ **ดูทะเบียนรับล่าสุด** — ดู ๒๐ รายการหลังสุดพร้อมลิงก์ไฟล์
- การ์ด **ทะเบียนรับล่าสุด** ที่หน้าแรก — ๒๐ รายการหลังสุด พร้อมปุ่ม **แก้ไข**

### 2.5 แก้ไขรายการที่เคยลงทะเบียนไว้

กด **แก้ไข** ที่รายการใดก็ได้ ทั้งจากการ์ดหน้าแรกและจากกล่อง "ดูทะเบียนรับล่าสุด"
ระบบจะดึงค่าที่บันทึกไว้ทุกช่องกลับมาลงฟอร์มให้ แล้วขอให้เลือกไฟล์ PDF
ของหนังสือฉบับนั้น พอประทับตราเสร็จกด **บันทึกทับรายการเดิม**
จะเขียนทับแถวเดิมในชีต และย้ายไฟล์ PDF ฉบับก่อนหน้าลงถังขยะของ Drive ให้

จุดที่ต้องรู้

- **ชีตไม่ได้เก็บตัวเอกสารที่ขยับตราได้** เก็บแต่ค่าที่กรอกในฟอร์มกับลิงก์ไฟล์
  ไฟล์ PDF ในทะเบียนเป็นฉบับที่ประทับตราลงไปแล้ว เปิดมาขยับตราไม่ได้
  ปุ่มแก้ไขจึงให้เลือกไฟล์ต้นฉบับมาประทับใหม่ ถ้าอยากเปิดงานเดิมทั้งชุด
  ให้ใช้ปุ่ม **บันทึกงานค้างไว้ (JSON)** บนแถบบนแทน
- ระหว่างอยู่ในโหมดแก้ไขจะมีแถบสีแดงในแผงฟอร์ม กด **ยกเลิก** เพื่อออกจากโหมด
  ถ้าไม่ออก การบันทึกครั้งถัดไปจะยังทับแถวเดิมอยู่
- แชร์เข้า LINE พร้อมติ๊ก "บันทึกลงทะเบียนรับด้วย" ระหว่างอยู่ในโหมดแก้ไข
  ก็จะทับแถวเดิมเช่นกัน ไม่เพิ่มแถวใหม่

**ชีตจะมีสองคอลัมน์เพิ่มขึ้นมาท้ายตาราง** ระบบเติมให้เองเมื่อเรียกใช้ครั้งแรก
ไม่ต้องไปเพิ่มเอง และไม่ต้องย้ายข้อมูลเก่า

| คอลัมน์ | ใช้ทำอะไร |
| --- | --- |
| รหัสเอกสาร | รหัสสุ่มประจำแถว ใช้อ้างอิงแทนเลขแถว แถวเก่าจะได้รหัสอัตโนมัติ |
| ข้อมูลดิบ | ค่าที่กรอกในฟอร์มทั้งชุดเก็บเป็น JSON ซ่อนไว้ ใช้ตอนดึงกลับมาแก้ไข |

> **ห้ามลบสองคอลัมน์นี้** ถ้าลบรหัสเอกสารทิ้ง ปุ่มแก้ไขจะหาแถวเดิมไม่เจอ
> และการบันทึกครั้งถัดไปจะกลายเป็นแถวใหม่
>
> คอลัมน์ "ข้อมูลดิบ" เป็นตัวจริงที่ใช้เติมกลับเข้าฟอร์ม ถ้าไปแก้ค่าในคอลัมน์
> ที่คนอ่าน (เช่น พิมพ์แก้ชื่อเรื่องในชีตตรง ๆ) ปุ่มแก้ไขจะยังดึงค่าเดิมมาให้
> อยู่ดี ต้องแก้ผ่านหน้าเว็บถึงจะตรงกันทั้งสองที่

---

## ส่วนที่ 3 — เข้าใช้งานผ่าน LINE และแชร์เอกสาร (LIFF)

หน้าเว็บจะตรวจ `liff.init()` และการล็อกอินก่อนเสมอ ผ่านแล้วจึงเข้าหน้าใช้งานปกติ
พร้อมดึงรูปและชื่อโปรไฟล์ LINE มาแสดงบนหัวเว็บ

### 3.1 สร้าง LIFF app

1. เข้า [LINE Developers Console](https://developers.line.biz/console/) เลือก Provider แล้วสร้าง **LINE Login channel**
2. ในแท็บ **LIFF** กด **Add**
   - **Endpoint URL** — ลิงก์หน้าเว็บจากส่วนที่ 1 (ต้องเป็น https และตรงทุกตัวอักษร)
   - **Size** — Full
   - **Scopes** — ติ๊ก `profile` และ `openid`
   - **Share target picker** — เปิด (**สำคัญ** ถ้าไม่เปิด ปุ่มแชร์จะขึ้นว่าใช้ไม่ได้)
3. คัดลอก **LIFF ID** ที่ได้

### 3.2 ใส่ LIFF ID ในหน้าเว็บ

เปิด `liff-share.js` บรรทัดบนสุด แล้วแทนค่าให้ตรงกับของตัวเอง

```js
const LIFF_ID = '1660731301-JRVEEAmW';
```

### 3.3 เปิดสิทธิ์ให้ผู้รับเปิดไฟล์ได้

การแชร์จะอัปโหลด PDF ขึ้น Drive แล้วส่งลิงก์ไปในการ์ด ผู้รับจึงต้องเปิดไฟล์ได้
ให้ตั้งค่าใน `Code.gs` เป็น

```js
SHARE_WITH_ANYONE: true
```

แล้ว **deploy ใหม่** (ถ้ายังเป็น `false` ผู้รับจะกดปุ่มแล้วเจอหน้าขอสิทธิ์เข้าถึง)

### 3.4 วิธีใช้

กด **แชร์เข้า LINE** ได้สองที่ คือไอคอน LINE บนแถบบนของหน้าทำงาน
หรือปุ่มท้ายแผง **กรอกข้อมูลลงรับ**

กล่องแชร์มีให้เลือกสองรูปแบบด้วยแถบด้านบน

| รูปแบบ | ผู้รับได้อะไร | เหมาะกับ |
|---|---|---|
| **การ์ดข้อมูล** (เริ่มต้น) | Flex Message สรุปข้อมูลลงรับ พร้อมปุ่มเปิด PDF และปุ่มส่งต่อ | ส่งให้อ่านและกดต่อ |
| **รูปหน้าเอกสาร** | รูปหน้าเอกสารที่ประทับตราแล้ว นำหน้าด้วยข้อความที่พิมพ์เอง | ให้เห็นตัวเอกสารทันทีในแชท |

รูปแบบที่เลือกล่าสุดจำไว้ในเครื่อง (`localStorage` คีย์ `saraban.shareMode.v1`)
ครั้งต่อไปเปิดกล่องแชร์ก็ขึ้นรูปแบบเดิม

โหมดการ์ดข้อมูลจะสร้างไฟล์ PDF อัปโหลดขึ้น Drive แล้วแสดงตัวอย่างการ์ดที่ผู้รับจะเห็น
กด **เลือกผู้รับใน LINE** เพื่อเลือกแชท กลุ่ม หรือเพื่อนที่จะส่งไปให้

การ์ดที่ส่งประกอบด้วย

- หัวการ์ดสีหมึกน้ำเงิน ชื่อโรงเรียน และเลขทะเบียนรับเป็นเลขไทย
- เรื่อง วันที่รับ เลขที่หนังสือ จาก ถึง ฝ่ายที่รับผิดชอบ การปฏิบัติ ตามที่กรอกในฟอร์ม
- กล่องเน้นการสั่งการและข้อความเพิ่มเติม
- ปุ่ม **เปิดเอกสาร PDF** ให้ผู้รับกดดูไฟล์ที่ประทับตราแล้ว
- ปุ่มสิ่งที่ส่งมาด้วยตามลิงก์ที่กรอกไว้ ถ้ามีมากกว่าหนึ่งรายการจะแยกเป็นการ์ดใบที่สองในชุดเดียวกัน

ช่องสิ่งที่ส่งมาด้วยกรอกได้ทั้ง **ชื่อรายการ** และ **ลิงก์** ชื่อที่กรอกจะกลายเป็นข้อความบนปุ่ม
ถ้าเว้นชื่อไว้จะใช้ว่า "สิ่งที่ส่งมาด้วย ๑ ๒ ๓" ตามลำดับ

**เลือกโทนสีการ์ดได้** ในกล่องแชร์มีปุ่มโทนสีสามแบบอยู่เหนือการ์ดตัวอย่าง

| โทน | คีย์ | สีหัวการ์ด |
|---|---|---|
| หมึกน้ำเงิน (เริ่มต้น) | `navy` | `#12203D` |
| ชาดแดง | `crimson` | `#75211D` |
| ครามม่วง | `plum` | `#2E2150` |

กดแล้วการ์ดตัวอย่างเปลี่ยนสีทันที โทนที่เลือกล่าสุดจำไว้ในเครื่อง
(`localStorage` คีย์ `saraban.shareTheme.v1`) ครั้งต่อไปเปิดกล่องแชร์ก็ขึ้นโทนเดิม

โทนสีเป็นของการ์ดใบที่ส่งครั้งแรกเท่านั้น ไม่ได้เดินทางไปกับลิงก์ปุ่มส่งต่อ
การ์ดทอดสองจึงยังเป็นสีเขียวเสมอ ความยาวลิงก์ไม่ได้รับผลกระทบ

อยากได้โทนที่สี่ ให้เติมอีกก้อนใน `THEMES` และอีกบรรทัดใน `ORIGIN_THEMES`
ที่ `flex-doc.js` ปุ่มบนหน้าจอสร้างจากรายการนั้น ไม่ต้องแก้ HTML

**หนังสือด่วนขึ้นสีแดง** ถ้าช่อง **การปฏิบัติ** มีคำว่า "ด่วน" อยู่ในข้อความ
(ด่วน ด่วนมาก ด่วนที่สุด หรือประโยคที่มีคำนี้) บรรทัดการปฏิบัติบนการ์ดจะขึ้นเป็น
ตัวหนาสีแดง `#B23A34` ทั้งหัวข้อและข้อความ ทั้งในการ์ดตัวอย่าง การ์ดต้นทาง
และการ์ดที่ถูกส่งต่อ ช่องอื่นยังคงสีตามโทนที่เลือกไว้ตามเดิม

### 3.5 ปุ่มส่งต่อบนการ์ด (ส่งต่อได้ทอดเดียว)

การ์ดต้นทางมีปุ่ม **ส่งต่อเอกสารนี้** สีเขียวติดไปด้วย ผู้รับกดแล้วจะเปิดหน้า `share.html`
ซึ่งประกอบการ์ดขึ้นมาใหม่ให้เลือกผู้รับคนถัดไปได้

การ์ดสองใบนี้ตั้งใจให้สีต่างกัน ดูปราดเดียวรู้ว่าอยู่ชั้นไหน

| | การ์ดต้นทาง | การ์ดที่ถูกส่งต่อ |
|---|---|---|
| โทนสี | เลือกได้สามโทน (ค่าเริ่มต้นหมึกน้ำเงิน `#12203D`) | เขียวหมึก `#146049` ตายตัว |
| หัวการ์ด | ลงรับหนังสือราชการ | ส่งต่อเอกสารลงรับ |
| ปุ่มส่งต่อ | มี | **ไม่มี** |

**เอกสารเดินทางได้แค่ทอดเดียว** งานสารบรรณ → ผู้รับคนแรก → ผู้รับปลายทาง แล้วจบ
เพราะการ์ดใบที่สองไม่มีปุ่มส่งต่ออีก ถ้าอยากกระจายต่อต้องกลับไปแชร์จากหน้าเว็บใหม่

**ผู้ส่งต่อเติมข้อสั่งการได้** ก่อนกดเลือกผู้รับ หน้า `share.html` มีช่องให้พิมพ์
ข้อสั่งการหรือบันทึกเพิ่มเติมได้ถึง ๓๐๐ ตัวอักษร (การ์ดตัวอย่างข้างบนอัปเดตตามที่พิมพ์ทันที)
ข้อความจะไปอยู่ในกล่องสีเขียวอ่อนท้ายการ์ด พร้อมชื่อผู้ส่งต่อจากบัญชี LINE
ถ้าไม่พิมพ์อะไรเลย กล่องนั้นจะขึ้นแค่ว่า "ส่งต่อโดย {ชื่อ}"

ข้อสั่งการตรงนี้ไม่ถูกจำกัดความยาวเหมือนข้อมูลในลิงก์ เพราะการ์ดชั้นสุดท้าย
ไม่ต้องเข้ารหัสข้อมูลกลับลง URL อีกแล้ว

**ข้อมูลเดินทางไปกับลิงก์** ไม่ได้เก็บในฐานข้อมูลใด ๆ ลิงก์หน้าตาแบบนี้

```
https://liff.line.me/{LIFF_ID}/share.html?d={ข้อมูลที่บีบอัดแล้ว}
```

จึงต้องตั้ง **Endpoint URL ของ LIFF ให้เป็นรากของเว็บ** (ลงท้ายด้วย `/` ไม่ใช่ `/index.html`)
เพราะ LINE จะเอา `/share.html` ไปต่อท้าย Endpoint URL ให้เอง

ถ้าอยากแยกเป็น LIFF คนละตัว ให้สร้าง LIFF อีกตัวชี้ที่ `.../share.html` แล้วใส่ LIFF ID นั้นทั้งสองที่

- `liff-share.js` → `const SHARE_PAGE_LIFF_ID = '...'`
- `share.js` → `const PAGE_LIFF_ID = '...'`

**ข้อจำกัดความยาว** LINE ยอมให้ลิงก์ในปุ่มยาวไม่เกิน ๑๐๐๐ ตัวอักษร ระบบจึงบีบอัดข้อมูลก่อน
(ปกติเอกสารทั่วไปใช้ราว ๔๐๐–๙๐๐ ตัวอักษร) ถ้ายังยาวเกินจะตัดข้อมูลรองออกตามลำดับ
คือข้อความเพิ่มเติม → การปฏิบัติ → เลขที่/วันที่หนังสือ → จาก/ถึง/ฝ่าย
โดยกล่องแชร์จะขึ้นเตือนให้ทราบก่อนเสมอ ปุ่มเปิด PDF และสิ่งที่ส่งมาด้วยไม่ถูกตัดทิ้ง

ติ๊ก **บันทึกลงทะเบียนรับด้วย** ในกล่องแชร์ ถ้าต้องการให้เขียนแถวลงชีตทะเบียนไปพร้อมกัน
โดยใช้ลิงก์ไฟล์เดิม ไม่อัปโหลดซ้ำ

### 3.6 ส่งเป็นรูปหน้าเอกสารพร้อมข้อความ

เลือกแถบ **รูปหน้าเอกสาร** ในกล่องแชร์ จะได้สามอย่างเพิ่มมา

- **ปุ่มเลือกหน้า** ติ๊กได้ว่าจะส่งหน้าไหนบ้าง ค่าเริ่มต้นคือทุกหน้าเท่าที่ส่งได้
- **ตัวอย่างรูป** ย่อหน้าที่ติ๊กไว้ให้ดูก่อนส่ง
- **ช่องข้อความ** พิมพ์ข้อความนำได้ถึง ๑๐๐๐ ตัวอักษร ระบบร่างให้จากฟอร์มก่อนแล้ว
  (ชื่อโรงเรียน เลขที่รับ เรื่อง การสั่งการ ข้อความเพิ่มเติม) แก้ทับได้ตามใจ
  หรือกด **ดึงข้อความจากฟอร์ม** เพื่อเขียนร่างใหม่ · เว้นว่างไว้ = ส่งเฉพาะรูป

กด **ส่งรูปเข้า LINE** แล้วเลือกผู้รับได้เหมือนกัน ข้อความจะไปถึงก่อน แล้วรูปตามไปทีละหน้า

**ส่งได้ครั้งละไม่เกิน ๕ กล่อง** เป็นข้อจำกัดของ LINE เอง ข้อความนำนับเป็นหนึ่งกล่องด้วย
พิมพ์ข้อความแล้วจึงเหลือที่ให้รูป ๔ หน้า ถ้าติ๊กไว้ครบ ๕ หน้าแล้วเพิ่งมาพิมพ์ข้อความ
ระบบจะเอาหน้าท้ายสุดออกให้พร้อมขึ้นแจ้ง เอกสารหนากว่านั้นให้แบ่งส่งหลายรอบ
หรือส่งเป็นการ์ดข้อมูลซึ่งแนบ PDF ทั้งเล่มไปในปุ่มเดียว

**รูปเปิดสาธารณะเสมอ** ข้อความรูปภาพของ LINE ไม่ได้แนบไฟล์ไปด้วย แต่ส่งไปแค่ลิงก์
แล้วเซิร์ฟเวอร์ของ LINE มาดึงรูปเอง ไฟล์รูปจึงถูกตั้งเป็น "ผู้ที่มีลิงก์เปิดได้" ทุกครั้ง
ไม่ว่า `SHARE_WITH_ANYONE` จะตั้งไว้อย่างไร **เอกสารลับจึงควรส่งเป็นการ์ดข้อมูลแทน**

รูปถูกเก็บแยกไว้ในโฟลเดอร์ย่อยชื่อ **ภาพแชร์เข้า LINE** ใต้โฟลเดอร์เดียวกับ PDF
(สร้างให้อัตโนมัติครั้งแรกที่ส่ง เปลี่ยนชื่อได้ที่ `CONFIG.IMAGE_FOLDER_NAME`)

โหมดนี้ไม่เขียนแถวลงทะเบียน ถ้าต้องการลงทะเบียนด้วยให้ใช้ปุ่มบันทึกในหน้าหลัก
หรือส่งเป็นการ์ดข้อมูลแล้วติ๊ก **บันทึกลงทะเบียนรับด้วย**

**เก็บกวาดรูปเก่า** รูปที่ส่งไปแล้วสะสมใน Drive เรื่อย ๆ ใน `Code.gs` มีฟังก์ชัน
`cleanOldShareImages()` ย้ายรูปที่เก่ากว่า `CONFIG.IMAGE_KEEP_DAYS` วัน (ค่าเริ่มต้น ๓๐)
ลงถังขยะ กดรันเองเป็นครั้งคราว หรือตั้งทริกเกอร์รายวันก็ได้ที่
**Apps Script → ทริกเกอร์ → เพิ่มทริกเกอร์ →** ฟังก์ชัน `cleanOldShareImages`
ตามเวลาที่ต้องการ

ระวังว่ารูปที่ถูกลบแล้วจะหายไปจากแชทเก่าด้วย เพราะ LINE ไปดึงจากลิงก์ทุกครั้งที่เปิดดู
ถ้าอยากให้รูปอยู่ถาวรให้ตั้ง `IMAGE_KEEP_DAYS: 0`

---

## ตั้งค่าชื่อโรงเรียน ผู้บริหาร และโลโก้

กดปุ่ม **⚙ ตั้งค่า** มุมขวาบนหน้าแรก (หรือหัวข้อ "ตั้งค่า" ในแผงเครื่องมือ) กรอกได้ทั้ง

| ช่อง | ใช้ที่ไหน |
|---|---|
| ชื่อโรงเรียน | หัวเว็บ ชื่อแท็บ บรรทัดแรกของตรารับ และบรรทัดตำแหน่งบนตราลงนาม |
| ชื่อ ผอ. + ตำแหน่ง | ตราลงนาม ผอ. และบรรทัด "เรียน ผู้อำนวยการ" บนตราสั่งการ |
| ชื่อรอง ผอ. + ตำแหน่ง | ตราลงนามรอง ผอ. |
| ลิงก์ภาพโลโก้ | โลโก้หัวเว็บ และบนตรารับถ้าติ๊กช่องไว้ |

**ค่าเหล่านี้เก็บที่ไหน** ถ้าตั้งค่าส่วนที่ 2 แล้ว ระบบจะบันทึกลงชีต **ตั้งค่าหน่วยงาน** ในสเปรดชีตเดียวกัน (สคริปต์สร้างชีตนี้ให้เองครั้งแรกที่ใช้) ตั้งครั้งเดียวทุกเครื่องที่เปิดระบบจะได้ค่าชุดเดียวกัน จะพิมพ์แก้ในชีตตรง ๆ ก็ได้ แล้วกด **ดึงค่าจากชีต** ในหน้าตั้งค่า ถ้ายังไม่ได้ตั้งส่วนที่ 2 ค่าจะเก็บไว้ในเบราว์เซอร์เครื่องนั้นเท่านั้น

> ถ้าเคยติดตั้ง `Code.gs` เวอร์ชันก่อนหน้า ต้องวางโค้ดใหม่แล้ว **deploy ใหม่** เมนูตั้งค่าถึงจะบันทึกลงชีตได้ ระหว่างที่ยังไม่ deploy ระบบจะแจ้งเตือนและเก็บค่าไว้ในเครื่องให้ก่อน

**โลโก้บนการ์ด LINE** หัวการ์ด Flex มีโลโก้เล็ก ๆ หน้าชื่อโรงเรียน ตัวนี้ฝังไว้เป็นค่าคงที่ `SCHOOL_LOGO` ในไฟล์ `flex-doc.js` ไม่ได้ดึงจากเมนูตั้งค่า เพราะลิงก์ปุ่มส่งต่อมีเพดานความยาว ๑๐๐๐ ตัวอักษร ถ้าเปลี่ยนโลโก้โรงเรียนให้แก้บรรทัดนั้นด้วยหนึ่งที่ (ต้องเป็นลิงก์ https ที่เป็นไฟล์ PNG หรือ JPEG)

**เรื่องโลโก้บนตรารับ** ภาพจากเว็บที่ไม่อนุญาตให้ดึงข้ามโดเมน (CORS) จะทำให้สั่งออก PDF ไม่ได้ ระบบจึงตรวจให้ตอนกดบันทึก ถ้าใช้ไม่ได้จะปิดตัวเลือกนี้อัตโนมัติ แต่ยังใช้เป็นโลโก้หัวเว็บได้ตามปกติ วิธีที่ชัวร์ที่สุดคือใช้ภาพจากบริการที่รองรับ CORS เช่น Cloudinary หรืออัปโหลดไว้ใน repo เดียวกับหน้าเว็บ

---

## หมายเหตุสำคัญ

**ทุกครั้งที่แก้ `Code.gs` ต้อง deploy ใหม่** เลือก "การทำให้ใช้งานได้ใหม่" ไม่ใช่แค่กดบันทึก มิฉะนั้นเว็บยังเรียกโค้ดเวอร์ชันเก่า

**เรื่อง `Content-Type`** ฝั่งเว็บส่งข้อมูลเป็น `text/plain` โดยตั้งใจ เพราะ Apps Script ไม่ตอบคำขอ preflight ของ CORS ถ้าเปลี่ยนไปใช้ `application/json` เบราว์เซอร์จะบล็อกก่อนถึงเซิร์ฟเวอร์

**ขนาดไฟล์** เอกสารสัก ๑๐ หน้าจะได้ PDF ราว ๒–๕ MB ซึ่งอยู่ในเกณฑ์ที่ Apps Script รับได้ ถ้าเอกสารหนามากแล้วอัปโหลดไม่ผ่าน ให้ลดค่า `quality` ใน `buildPdf()` ของ `app.js` จาก `0.92` ลงเป็น `0.8`

**การแชร์ไฟล์** ค่าเริ่มต้น `SHARE_WITH_ANYONE: false` คือไฟล์เปิดได้เฉพาะคนที่มีสิทธิ์ในโฟลเดอร์ ถ้าต้องการส่งลิงก์ให้ครูท่านอื่นเปิดได้ทันทีให้เปลี่ยนเป็น `true` แต่ควรระวังเอกสารที่มีข้อมูลส่วนบุคคล

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุที่พบบ่อยที่สุด |
|---|---|
| ติดต่อระบบทะเบียนไม่ได้ | ตอน deploy ไม่ได้เลือก "ผู้ที่มีสิทธิ์เข้าถึง: ทุกคน" |
| API key ไม่ถูกต้อง | `apiKey` ใน `app.js` ไม่ตรงกับ `API_KEY` ใน `Code.gs` |
| ตอบกลับไม่ถูกรูปแบบ | แก้ `Code.gs` แล้วลืม deploy ใหม่ |
| บันทึกตั้งค่าลงชีตไม่สำเร็จ | ใช้ `Code.gs` เวอร์ชันเก่าที่ยังไม่มีคำสั่ง `saveSettings` ให้วางโค้ดใหม่แล้ว deploy ใหม่ |
| หน้าแรกไม่มีการ์ดทะเบียน | ยังไม่ได้ตั้ง `CLOUD.webAppUrl` ใน `app.js` |
| กดแก้ไขแล้วขึ้นว่าไม่พบเอกสาร | แถวนั้นถูกลบจากชีต หรือคอลัมน์ "รหัสเอกสาร" ถูกลบทิ้ง |
| กดแก้ไขแล้วกล่องเลือกไฟล์ไม่เด้ง | เบราว์เซอร์บล็อกเพราะรอผลจากเซิร์ฟเวอร์ก่อน ให้กดที่กล่องลากไฟล์เอง |
| แก้ไขแล้วได้แถวใหม่แทนที่จะทับ | ใช้ `Code.gs` เวอร์ชันเก่าที่ยังไม่รับ `docId` ให้วางโค้ดใหม่แล้ว deploy ใหม่ |
| เลขที่รับกลายเป็น 124 แทน 0124 | แถวที่บันทึกก่อนอัปเดตนี้ Sheets กินศูนย์นำหน้าไปแล้ว แก้เลขในหน้าเว็บแล้วบันทึกทับหนึ่งครั้ง |
| เปลี่ยนชื่อในชีตแล้วหน้าเว็บยังเป็นชื่อเดิม | เปิดเมนูตั้งค่าแล้วกด "ดึงค่าจากชีต" หรือรีเฟรชหน้าเว็บ |
| ยังไม่ได้ตั้งค่า FOLDER_ID | ยังไม่ได้แทนค่าโฟลเดอร์ใน `CONFIG` |
| เริ่มต้น LIFF ไม่สำเร็จ | Endpoint URL ใน LIFF ไม่ตรงกับที่อยู่จริงของหน้าเว็บ หรือ LIFF ID ผิด |
| ปุ่มแชร์บอกว่ายังไม่ได้เปิด share target picker | ยังไม่ได้เปิดสวิตช์นี้ในหน้า LIFF ของ LINE Developers Console |
| ผู้รับกดปุ่มเปิด PDF แล้วเจอหน้าขอสิทธิ์ | `SHARE_WITH_ANYONE` ยังเป็น `false` หรือแก้แล้วแต่ยังไม่ deploy ใหม่ |
| แชร์แล้วการ์ดไม่มีข้อมูล | ยังไม่ได้กรอกแผง "กรอกข้อมูลลงรับ" ก่อนกดแชร์ |
| กดปุ่มส่งต่อแล้วเปิดหน้าตัวแก้เอกสารแทน | Endpoint URL ของ LIFF ไม่ได้ลงท้ายด้วย `/` ทำให้ต่อ `/share.html` ไม่ติด |
| หน้าส่งต่อบอกว่าอ่านข้อมูลในลิงก์ไม่ได้ | ลิงก์ถูกตัดกลางทาง หรือเบราว์เซอร์เก่าที่ไม่มี DecompressionStream |
| การ์ดที่ส่งต่อมีข้อมูลไม่ครบ | ข้อมูลยาวเกิน ๑๐๐๐ ตัวอักษร ระบบตัดฟิลด์รองออกให้พอดี |
| ส่งรูปแล้วผู้รับเห็นเป็นช่องว่าง | ใช้ `Code.gs` เวอร์ชันเก่าที่ยังไม่มีคำสั่ง `saveShareImages` ให้วางโค้ดใหม่แล้ว deploy ใหม่ |
| ติ๊กหน้าที่ห้าไม่ได้ | มีข้อความนำอยู่ ข้อความนับเป็นหนึ่งกล่องจากโควตาห้ากล่องของ LINE |
| รูปในแชทเก่าหายไป | `cleanOldShareImages()` ลบรูปเก่าไปแล้ว ตั้ง `IMAGE_KEEP_DAYS: 0` ถ้าอยากให้อยู่ถาวร |
| เปิดโฟลเดอร์ไม่ได้ | รหัสโฟลเดอร์ผิด หรือบัญชีที่ deploy ไม่มีสิทธิ์ในโฟลเดอร์นั้น |
